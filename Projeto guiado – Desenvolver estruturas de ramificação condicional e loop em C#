string[] frases = {"O Giovani. é um. macaco fidido", "sem. mas!"};

int gps = 0;

for (int i = 0; i < 2; i++)
{
    string fraseAtual = frases[i];
    gps = fraseAtual.IndexOf(".");

    string minhaFrase;

    int count = 0;
    while(gps != -1)
    { 
        count++;
        if (count > 5) break;s
        minhaFrase = fraseAtual.Remove(gps);
        

        Console.WriteLine(minhaFrase);

    }
}
